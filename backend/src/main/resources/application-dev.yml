# =============================================================================
# AuraFit – DEV Profile (Local Docker PostgreSQL)
# =============================================================================
# Activated by: --spring.profiles.active=dev   (default in application.yml)
# Requires:     docker compose up -d
# =============================================================================

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/aurafit_db
    username: aurafit
    password: aurafit123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update                    # Auto-create/alter tables in dev
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # -- Gmail SMTP (App Password) -----------------------------------------------
  mail:
    host: smtp.gmail.com
    port: 587
    username: hoangbui317000@gmail.com
    password: eoqyyurgrdvttqct
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
